{
  "name": "crozzo-pos",
  "private": true,
  "version": "1.0.9",
  "type": "module",
  "scripts": {
    "serve": "serve src -l 1420",
    "tauri": "tauri",
    "postinstall": "node scripts/copy-updater.mjs",
    "dev": "tauri dev",
    "build": "tauri build",
    "rebuild": "npm run tauri build -- -- --force"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "serve": "^14"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-updater": "^2"
  }
}
